ジャンル検索
<% @genres.each do |genre| %>
  <%= genre.name %>
<% end %>

<% @items.each do |item| %>
  <%= link_to item_path(item) do %>
    <%= image_tag item.image if item.image.attached? %>
  <% end %>

  <%= item.name %>
  <%= item.price %>
<% end %>